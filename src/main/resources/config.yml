# ==========================================================
#  BuildFFA Configuration
#  Created by Muvixo
#  Plugin author: VanSaMa
#  Made by PixelValley
#  Version: 4.0 (1.8.8 Compatible) - SQLite Edition
# ==========================================================
#
#  This file is AUTO-MERGED on plugin update.
#  New keys are added automatically.
#  Your existing values are NEVER overwritten.
# ==========================================================

# ----------------------------------------------------------
#  Void Kill Height
#  Players who fall below this Y level will die or teleport.
#  Set with /buildffa setvoid [y]
# ----------------------------------------------------------
kill-height: -13.0

# ----------------------------------------------------------
#  High Build Limit
#  Players cannot place/break blocks above this Y level.
#  Set with /buildffa sethighlimit [y]
# ----------------------------------------------------------
high-limit: 53.0

# ----------------------------------------------------------
#  Block Timings (seconds)
#  How long blocks take to restore / disappear.
# ----------------------------------------------------------
blocks:
  # Natural blocks (stone, dirt, wood…) broken by players
  # reappear after this many seconds.
  natural-restore-seconds: 9

  # Player-placed blocks disappear after this many seconds.
  # (Ignored when the player is in Build Mode.)
  placed-decay-seconds: 5

# ----------------------------------------------------------
#  Combat Mode
#  When two players hit each other, they enter combat.
#  If one leaves during combat, the other is healed.
# ----------------------------------------------------------
combat:
  # Seconds after the last hit until combat ends
  timeout-seconds: 15

  # Message shown to the opponent who stayed (private)
  # %player% = the player who left
  opponent-left-message: '&a%player% &7left the server during combat. &aYou were healed.'

# ----------------------------------------------------------
#  Void Behaviour
# ----------------------------------------------------------
void:
  # If true  -> teleport to spawn + full heal (HP only),
  #            BUT death is still registered in stats.
  # If false -> kill the player normally (death also counted).
  teleport-instead-of-kill: true

  # Delay before teleporting (in ticks, 20 ticks = 1 sec)
  teleport-delay: 0

  # Private message shown to the player who fell.
  # Leave empty ('') to disable the private message.
  # %player% = player name
  teleport-message: ''

  # Broadcast message when NOBODY is credited
  # %player% = player name
  death-message: '&c%player% &7fell into the void'

  # Broadcast message when someone knocked the player in
  # (they were hit in the last 10 seconds)
  # %player% = victim name
  # %killer% = attacker who last hit them
  killed-by-message: '&c%player% &7was knocked into the void by &c%killer%'

# ----------------------------------------------------------
#  YPvP (Y-Level PvP Restriction)
#  Players at or above this Y level cannot hit or be hit.
#  Set with /buildffa ypvp [y]
# ----------------------------------------------------------
ypvp:
  enabled: true
  y-level: 61.5
  block-projectiles: true

  # Message shown to the player when they try to PvP above the limit.
  #   %y%  = the configured Y level (int)
  #
  # Examples:
  #   '&cYou cannot PvP here!'
  #   '&cYou cannot PvP here! &7(&eY=%y%&7)'
  #   ''   <- disables the message entirely
  message: '&cYou cannot PvP here!'

# ----------------------------------------------------------
#  YPearl (Y-Level Ender Pearl Restriction)
#  Players below this Y level can throw pearls.
#  The moment a pearl crosses ABOVE this Y level, it is
#  deleted mid-air and the thrower is told.
#  Set with /buildffa ypearl [y]
# ----------------------------------------------------------
ypearl:
  enabled: true
  y-level: 61.5

  # Message shown to the player when their pearl is blocked/deleted.
  #   %y%  = the configured Y level (int)
  #
  # Examples:
  #   '&cYou cannot throw pearls here!'
  #   '&cYou cannot throw pearls here! &7(&eY=%y%&7)'
  #   ''   <- disables the message entirely
  message: '&cYou cannot throw pearls here!'

# ----------------------------------------------------------
#  Database (SQLite)
#  All data is stored in a single file: buildffa.db
#  Contains: players stats + kits (base64 serialized)
#
#  Old files (db/*.json, kits/*.yml) are AUTO-MIGRATED
#  on first startup and then renamed to *_legacy_*
# ----------------------------------------------------------
database:
  # Automatically save all player data periodically
  autosave: true

  # Save interval in SECONDS (300 = 5 minutes)
  # Minimum allowed value: 30 seconds
  autosave-interval: 300

# ----------------------------------------------------------
#  Killstreak Rewards
#  When a player reaches a certain killstreak, they receive
#  a reward bundle instead of just a single Golden Apple.
#
#  ITEM types:
#    gapple   = Golden Apple
#    fb       = Fireball (Fire Charge) — right-click to shoot
#    perl     = Ender Pearl
#    feather  = Feather (double-jump, right-click in air)
#    speed:N  = Potion of Swiftness (N = level 1-2)
#    jump:N   = Potion of Leaping (N = level 1-5)
#
#  JUMP LEVELS (1.8.8):
#    jump:1 = Jump Boost I
#    jump:2 = Jump Boost II
#    jump:3 = Jump Boost III
#    jump:4 = Jump Boost IV
#    jump:5 = Jump Boost V  ← strongest
#
#  ⚠️ CYCLE BEHAVIOR:
#    With repeat-from-12: true, killstreaks > 12 wrap around:
#      KS 13 → level 1 → no reward → only default gapple
#      KS 14 → level 2 → no reward → only default gapple
#      KS 15 → level 3 → no reward → only default gapple
#      KS 16 → level 4 → reward 4 ✅
#      KS 24 → level 12 → reward 12 ✅
#      KS 25 → level 1 → cycle restarts
# ----------------------------------------------------------
killstreak-rewards:
  enabled: true

  # If true, rewards repeat every 12 killstreaks after level 12
  repeat-from-12: true

  # If true, EVERY reward from 1 up to the streak is given (stacking).
  # If false, only the highest matching reward is given.
  stacking: false

  # Rewards per killstreak level
  rewards:
    '4':   'gapple:1 fb:1 speed:1'
    '5':   'gapple:2 speed:2 jump:1'
    '6':   'gapple:2 fb:1 jump:5 speed:1'
    '7':   'perl:1 gapple:1 speed:2'
    '8':   'gapple:1 fb:1'
    '9':   'gapple:1 perl:1'
    '10':  'gapple:2 fb:1 feather:1 perl:1 speed:2 jump:5 jump:3'
    '11':  'feather:1 speed:2 jump:5 perl:1'
    '12':  'feather:1 jump:5 perl:1 fb:1'

# ----------------------------------------------------------
#  Feather Jump (Double Jump)
#  Right-click a Feather while in the air to double-jump.
#  The feather is consumed.
#  3-second cooldown per player.
# ----------------------------------------------------------
feather-jump:
  # Upward velocity (Y axis)
  # 0.5 = very soft
  # 0.7 = soft
  # 0.9 = normal (recommended)
  # 1.2 = strong
  # 1.5 = extreme
  boost: 1.2

  # Forward velocity (in the direction the player is looking)
  # 0.0 = no forward push (straight up)
  # 0.2 = small push
  # 0.4 = noticeable push
  forward-boost: 0.0

  # Projectile settings (used when launching as a projectile)
  # If you want Feather to be thrown like a Snowball instead of a boost:
  #   set boost: 0.0 and speed: 2.0
  speed: 2.0
  knockback-horizontal: 1.5
  knockback-vertical: 0.9
  damage: 2.0

  # Message shown to the player (private)
  # Leave empty ('') to disable
  message: '&b✦ &fDouble Jump!'

# ----------------------------------------------------------
#  Fireball
#  Right-click a Fire Charge to shoot a real fireball.
#  Works in Survival mode (vanilla only works in Creative).
# ----------------------------------------------------------
fireball:
  # Let fireballs break blocks like true vanilla ghast fireballs.
  # false (default) = BedWars/Minemen style: knockback + damage only,
  # map/terrain is never destroyed.
  break-blocks: false

  # Message shown to the shooter (private)
  # Leave empty ('') to disable
  message: ''

  # Speed slider value (-10 to +10)
  # Default 1.5 → multiplier 1.15x (tiny boost over vanilla)
  speed-level: 1.5

  # Explosion size (blocks)
  yield: 1.0

  # Throw effects — applied to the shooter when they fire
  throw-effects:
    enabled: false
    effects:
      - 'SPEED:60:1'

  # Knockback + damage settings
  knockback:
    enabled: true

    # Radius (in blocks) where players get knocked
    radius: 4.0

    # Vertical knockback force
    height-force: 1.0

    # Horizontal knockback force
    radius-force: 1.5

    # Extra damage on top of the distance-based damage
    damage: 0.5

# ----------------------------------------------------------
#  Messages
#  Normal PvP kill message
#  %killer% = killer name (GREEN)
#  %loser%  = victim name (RED)
# ----------------------------------------------------------
kill: '&a%killer% &7killed &c%loser%'

Title-Suffix: '  &7Kill'
SubTitle-kill: ' &e+1 Kill'
join-message: ' &e%player%  &7joined the game  &8( &e%online% &7/ &e100 &8)'
quit-message: ' &e%player%  &7left the game  &8( &e%online% &7/ &e100 &8)'

# ----------------------------------------------------------
#  Infinite Food & Blocks
#
#  anti-hunger: player never gets hungry
#  ⚠️ NOTE: With this enabled, food does NOT heal HP.
#     Only Golden Apples / Potions can heal HP.
# ----------------------------------------------------------
infinite:
  food: true
  blocks: true

# ----------------------------------------------------------
#  Kill Heal (BedWars-style)
#  When a player kills someone, they receive:
#    - Full HP (or configurable amount)
#    - Fire reset
#    - Fall distance reset
#    - Optional Absorption hearts
#
#  ⚠️ Food is NOT refilled here — that's handled by
#     infinite.food above.
# ----------------------------------------------------------
kill-heal:
  # Enable/disable the heal-on-kill
  enabled: true

  # If true, killer's HP is set to max
  # If false, `amount` is added instead
  full-heal: true

  # Only used when full-heal: false
  # Example: 6.0 = 3 hearts
  amount: 6.0

  # Absorption hearts (golden bonus hearts)
  # Only works on 1.6+
  absorption:
    enabled: false
    # 0 = 2 hearts, 1 = 4 hearts, 2 = 6 hearts, etc.
    level: 1
    # Duration in seconds
    duration: 5

# ==========================================================
#  ⚠️  OWN HealthBar — DISABLED
# ==========================================================
#  This used to show YOUR OWN HP above the hotbar
#  (e.g. "❤ 20/20"). It is now fully disabled because
#  the ENEMY health bar (below) handles all PvP health display.
#
#  → To re-enable your own HP bar, set enabled: true
#    AND make sure HealthBarManager is instantiated in BuildFFA.java
# ==========================================================
healthbar:
  # DISABLED by default — own HP bar no longer shown
  enabled: false

  # Update interval in ticks (20 = 1 second)
  # 5 = smooth, 10 = medium, 20 = every second
  update-interval: 5

  # Mode: NUMERIC | HEARTS | PERCENT
  mode: NUMERIC

  # Format for NUMERIC and PERCENT modes
  #   %current%  ->  current HP
  #   %max%      ->  max HP
  #   %percent%  ->  HP percent (0-100)
  #   %player%   ->  player name
  format: '&c❤ &f%current%&7/&f%max%'

  # Colors for HEARTS mode
  filled-color: '&c'
  empty-color: '&7'
  heart-char: '❤'

# ==========================================================
#  ✅ Enemy HealthBar (ActionBar)
# ==========================================================
#  Shows the HP of the player you LAST HIT, above your hotbar.
#  Perfect for PvP — you see enemy HP without cluttering the screen.
#
#  HOW IT WORKS:
#    - You hit someone → their HP appears in your action bar
#    - You hit again     → HP refreshes
#    - You stop hitting → bar disappears after `timeout-seconds`
#
#  PLACEHOLDERS:
#    %name%  ->  enemy player name
#    %hp%    ->  enemy current HP
#    %max%   ->  enemy max HP
#
#  FORMAT EXAMPLES:
#    '&c[%name%] &8-- &f[%hp%&7/&f%max%&f]'   →  [Notch] -- [18/20]
#    '&c%name% &8» &f%hp%&7/&f%max%'           →  Notch » 18/20
#    '&e❤ &f%name% &7- &c%hp%&7/&c%max%'       →  ❤ Notch - 18/20
#    '&8[&c%name%&8] &f%hp%&7/&f%max%'         →  [Notch] 18/20
# ==========================================================
enemy-healthbar:
  # Enable/disable the enemy HP action bar
  enabled: true

  # Update interval in ticks (20 ticks = 1 second)
  # 5 = smooth (recommended), 10 = medium
  update-interval: 5

  # After this many seconds without hitting anyone,
  # the enemy HP bar disappears automatically.
  timeout-seconds: 5

  # Format string — see examples above
  format: '&c[%name%] &8-- &f[%hp%&7/&f%max%&f]'

# ==========================================================
#  Nametag HP Display (BELOW player's name)
# ==========================================================
#  Shows live HP below each player's name (like BedWars).
#
#  ✅ Uses HEALTH criteria → client auto-renders "20 ❤"
#  ✅ Uses pure NMS packets (safe on 1.8.8)
#  ✅ No prefix/suffix → no "22 > 16" disconnect
#
#  IMPORTANT:
#    On 1.8.8 the client ALWAYS renders the format as:
#         {display-name} {score}
#    The heart ❤ AFTER the number is added automatically
#    by the vanilla client when the objective uses the
#    "health" criteria and display-name is EMPTY.
#
#  display-name: the text shown BEFORE the HP number
#    ''            → "20 ❤"   ✅ (exactly like the image)
#    '&c❤'         → "❤ 20"
#    '&cHP'        → "HP 20"
#    '&7[&c❤&7]'   → "[❤] 20"
# ==========================================================
nametag:
  enabled: true
  update-interval: 10
  # خالی = فقط عدد + قلب خودکار وانیلا → "20 ❤"
  display-name: ''

# ----------------------------------------------------------
#  Permissions
# ----------------------------------------------------------
permissions:
  ypvp: buildffa.ypvp
  ypvp-bypass: buildffa.ypvp.bypass
  ypearl: buildffa.ypearl
  ypearl-bypass: buildffa.ypearl.bypass
  fireball-speed: buildffa.fireball.speed
  kiteditor: buildffa.kiteditor
  setvoid: buildffa.setvoid
  sethighlimit: buildffa.sethighlimit
  setspawn: buildffa.setspawn
  buildmode: buildffa.buildmode
  reload: buildffa.reload
  scoreboard-toggle: buildffa.scoreboard.toggle
  highlimit-bypass: buildffa.highlimit.bypass
  resetstats: buildffa.resetstats

messages:
  no-permission: '&cYou do not have permission to do this.'

# ----------------------------------------------------------
#  Kill Screen (Title/Subtitle on kill)
# ----------------------------------------------------------
kill-screen:
  enable-title: true
  title: '&e+%killcount% &7Kill'
  subtitle: '&e+1 Kill'
  enable-chat-fallback: false
  chat-message: '&a+1 Kill!'

# ----------------------------------------------------------
#  Sounds
#  Sound effects for kills, deaths, joins
#  NOTE: No sound on hits (too spammy)
# ----------------------------------------------------------
sounds:
  enabled: true
  kill: true
  death: true
  join: true
  chat: false
  volume: 1.0
  pitch: 1.0

# ----------------------------------------------------------
#  Spawn
#  Set with /buildffa setspawn
# ----------------------------------------------------------
spawn:
  world: world
  x: -83.61916298668011
  y: 69.46495898148244
  z: 90.46339979325055
  yaw: 90.342545
  pitch: 5.961797

# ----------------------------------------------------------
#  Leaderboard Refresh (used by hologram countdown)
#  %buildffa_next_update% counts down from this value
# ----------------------------------------------------------
leaderboard-refresh:
  interval-seconds: 600